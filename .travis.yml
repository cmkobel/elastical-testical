language: c
os: linux
compiler:
        - gcc

addons:
  apt:
    sources:
      - george-edison55-precise-backports
    packages:
      - cmake-data
      - cmake

script:
        - mkdir -p hmmmlib/build && cd hmmmlib/build/ && cmake .. && make




