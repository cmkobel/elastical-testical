language: c
os:
        - linux
        - osx
compiler:
        - gcc
        - clang

addons:
  apt:
    sources:
      - george-edison55-precise-backports
    packages:
      - cmake-data
      - cmake

script:
        - cd hmmmlib/build; rm CMakeCache.txt; rm -r CMakeFiles/; rm cmake_install.cmake; rm libHMMLIB.so*; rm Makefile; cmake .. &&  make





